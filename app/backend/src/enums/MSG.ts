enum MSG {
  INVALID_EMAIL = 'Incorrect email or password',
  INVALID_PASSWORD = 'Incorrect email or password',
  BLANK_EMAIL = 'All fields must be filled',
  BLANK_PASSWORD = 'All fields must be filled',
}

export default MSG;
